# 贡献指南

1. Issue 中说明问题、使用场景、可复现步骤和安全影响。
2. 分支命名使用 `feat/`、`fix/`、`docs/`；一个提交只解决一个问题。
3. 不得提交真实政务数据、个人信息、密码、令牌或未获许可的模型权重。
4. 提交前运行 `python3 -m unittest discover -s tests -v`。
5. 新增有副作用的工具必须声明权限、展示操作预览，并在执行前要求人工确认。

