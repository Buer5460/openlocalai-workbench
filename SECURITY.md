# 安全政策

请勿在公开 Issue 披露可利用漏洞或真实敏感数据。通过仓库 Security Advisory 私下报告，并包含受影响版本、复现条件和建议修复。

v0.2 Alpha 默认仅回环地址，提供本机管理员、scrypt 密码哈希、限速登录、12 小时会话、HttpOnly/SameSite Cookie、同源校验和 CSRF 防护。它仍是单管理员试点版本，**不具备政企生产安全条件**，不得直接暴露到公网。

生产化至少需要：统一身份认证、细粒度最小权限、TLS、存储加密、密钥管理、审计防篡改、备份恢复、依赖与镜像治理、漏洞响应，以及由部署单位按实际数据和网络边界开展等保、密评和数据合规评估。
